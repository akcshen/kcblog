---
title: 虚拟列表实现原理与优化方案
date: 2026-04-07 20:27:47
permalink: /pages/b91b18/
categories:
  - 收藏
  - 面试
tags:
  - 
---

# Vue3 虚拟列表实现原理与优化方案

## 一、什么是虚拟列表

虚拟列表（Virtual List）是一种性能优化技术，用于处理大量数据的滚动展示场景。其核心思想是：

- **只渲染可见区域的元素**，而非渲染全部数据
- 通过**撑高容器**保持滚动条功能正常
- 根据滚动位置**动态替换可见区域内容**

 Without virtualization: 1000 items = 1000 DOM nodes  
 With virtualization: 1000 items = ~20 DOM nodes (visible only)

## 二、实现原理

### 2.1 核心架构

```
┌─────────────────────────────┐
│   容器 (fixed height)       │
│  ┌───────────────────────┐  │
│  │  内部占位容器           │  │
│  │  (totalHeight high)   │  │
│  │                       │  │
│  │  ┌─────────────┐      │  │
│  │  │  Item 0     │ ←visible
│  │  └─────────────┘      │  │
│  │  ┌─────────────┐      │  │
│  │  │  Item 1     │ ←visible
│  │  └─────────────┘      │  │
│  │  ┌─────────────┐      │  │
│  │  │  Item 2     │ ←visible
│  │  └─────────────┘      │  │
│  │  (hidden items...)    │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 2.2 关键技术点

#### 1. 高度撑开

使用两个容器：

- **外层容器**：固定高度 + `overflow-y: auto`，负责滚动
- **内层占位容器**：高度 = 所有元素高度之和 + `position: relative`，撑开滚动条

```javascript
// 滚动时，容器的 scrollTop 与占位容器的总高度配合，实现正常滚动行为
<div class="virtual-list" ref="containerRef">     <!-- 滚动发生在这里 -->
  <div ref="innerRef" :style="{ height: totalHeight + 'px' }">  <!-- 占位 -->
    <div v-for="visibleItems" :style="{ position: 'absolute', top: position }" />
  </div>
</div>
```

#### 2. 元素定位

可见元素使用 `position: absolute` 定位，通过 `top` 属性计算每个元素的精确位置：

```javascript
position = 前面所有元素高度之和
top = position
```

#### 3. 滚动计算

滚动时的计算流程：

```
1. 获取 scrollTop (当前滚动位置)
2. 从第 0 项开始累加高度，找到第一个 > scrollTop 的元素 → start index
3. 从 start index 开始累加高度，找到第一个 > clientHeight 的元素 → end index
4. 渲染 [start, end] 范围内的元素
```

### 2.3 不固定高度处理

由于每个元素高度可能不同，无法使用固定高度计算。解决方案：

1. **首次测量缓存**：首次访问元素时，通过临时 DOM 测量真实高度
2. **缓存到对象**：将高度存储在 `heights[index]` 中，避免重复测量

```javascript
const calculateElementHeight = (index) => {
  // 1. 优先使用缓存
  if (heights.value[index] !== undefined) {
    return heights.value[index];
  }
  
  // 2. 临时创建元素测量高度
  const temp = document.createElement('div');
  // ... 设置样式并测量
  const height = temp.offsetHeight;
  
  // 3. 缓存结果
  heights.value[index] = height;
  return height;
};
```

## 三、代码实现

### 3.1 响应式数据

```javascript
const containerRef = ref(null);      // 滚动容器
const innerRef = ref(null);          // 内部占位容器
const items = ref([]);               // 全部数据
const visibleItems = ref([]);        // 可见元素
const heights = ref({});             // 高度缓存
const totalHeight = ref(0);          // 总高度
```

### 3.2 滚动处理函数

```javascript
const handleScroll = () => {
  const scrollTop = container.scrollTop;
  const clientHeight = container.clientHeight;
  
  // 1. 找到起始位置
  let offset = 0;
  for (let i = 0; i < items.length; i++) {
    if (offset + heights[i] > scrollTop) {
      start = i;
      break;
    }
    offset += heights[i];
  }
  
  // 2. 找到结束位置
  let visibleHeight = 0;
  for (let i = start; i < items.length; i++) {
    visibleHeight += heights[i];
    if (visibleHeight > clientHeight) {
      end = i;
      break;
    }
  }
  
  // 3. 生成可见项列表
  visibleItems = items.slice(start, end + 1);
};
```

## 四、优化方案

### 4.1 性能优化

#### 1. 函数节流

滚动事件触发频繁，添加节流避免频繁计算：

```javascript
const throttle = (fn, delay) => {
  let lastTime = 0;
  return () => {
    const now = Date.now();
    if (now - lastTime > delay) {
      lastTime = now;
      fn();
    }
  };
};

const handleScroll = throttle(() => {
  // ... 滚动处理逻辑
}, 50); // 50ms 节流
```

#### 2. 懒加载测量

不预先测量所有高度，而是在需要时才测量：

```javascript
// 初始化时不调用 calculateTotalHeight
// 而是在 handleScroll 中按需计算
```

#### 3. 回收机制

当元素滚动出视口后，可以延迟销毁（可选）：

```javascript
// 可以考虑保留最近访问的元素缓存
// 既保证性能，又避免内存浪费
```

### 4.2 用户体验优化

#### 1.骨架屏

数据加载时显示骨架屏：

```vue
<div v-if="!items.length" class="skeleton">
  <!-- 骨架屏内容 -->
</div>
```

#### 2. 预渲染缓冲区

多渲染几个额外元素，避免滚动时出现白屏：

```javascript
// 原始计算
const bufferSize = 5; // 缓冲区大小
for (let i = start - bufferSize; i <= end + bufferSize; i++) {
  if (i >= 0 && i < items.length) {
    result.push(items[i]);
  }
}
```

#### 3. 平滑过渡

添加动画效果：

```css
.virtual-list-item {
  transition: transform 0.1s ease-out;
}
```

### 4.3 内存优化

#### 1. 高度缓存策略

- 超过一定数量后，清除旧缓存
- 使用 LRU 策略保留热点数据

#### 2. 数据分页

超大数据集考虑分页加载：

```javascript
const loadMore = () => {
  // 滚动到底部时加载更多
  if (scrollTop + clientHeight > totalHeight - threshold) {
    // 加载下一页
  }
};
```

### 4.4 精确性优化

#### 1. 窗口 resize 处理

监听窗口大小变化，重新计算高度：

```javascript
onMounted(() => {
  window.addEventListener('resize', handleScroll);
});

onBeforeUnmount(() => {
  window.removeEventListener('resize', handleScroll);
});
```

## 五、性能对比

| 方案     | 1000 条数据 | 10000 条数据 |
| -------- | ----------- | ------------ |
| 普通渲染 | ~50ms       | ~500ms       |
| 虚拟列表 | ~5ms        | ~10ms        |

**DOM 节点数量对比：**

- 普通渲染：1000+ 个 DOM 节点
- 虚拟列表：约 20-30 个 DOM 节点（仅可见区域）

## 六、适用场景

### 适合使用虚拟列表

- 聊天记录展示
- 消息列表
- 商品列表
- 数据表格

### 不适合虚拟列表

- 元素高度完全不确定（如富文本）
- 需要频繁插入/删除中间元素
- 元素数量较少（< 50）

## 七、参考实现

- [Vue Virtual Scroll List](https://github.com/j_PANEL/vue-virtual-scroll-list)
- [React Virtualized](https://github.com/bvaughn/react-virtualized)
- [TanStack Table](https://tanstack.com/table) - 支持虚拟滚动的表格库
